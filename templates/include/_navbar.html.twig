<nav class="navbar container-fluid navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="/">Accueil</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
        <ul class="navbar-nav">
            <li class="nav-item d-flex">
                <a class="nav-link title-light" href="{{ path('app_index', {'_locale':'fr'}) }}">Fr</a>
                <a class="nav-link title-light" href="{{ path('app_index', {'_locale':'en'}) }}">En</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-secondary" href="/programs/">{{ 'Series'|trans }}<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-secondary" href="/categories/">{{ 'Categorie'|trans }}</a>
            </li>
            <div class="dropdown">
                <a class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Categories
                </a>
                    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                    {{ render(controller('App\\Controller\\HomeController::navbarTop')) }}
                    </div>
            </div>
            <li class="nav-item">
                {% if app.user %}
                    {{ app.user.email }}
                    <a href="{{ path('app_logout') }}">{{ 'loggout'|trans }}</a>
                    <a href="{{ path('profil') }}">{{ 'My account'|trans }}</a>
                {% else %}
                    <a href="{{ path('app_login') }}">{{ 'sign_in'|trans }}</a>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>
