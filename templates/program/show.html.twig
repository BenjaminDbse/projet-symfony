{% extends 'base.html.twig' %}

{% block title %}Série #{{ program.id }}{% endblock %}

{% block body %}
    <div class="media">
        <img class="align-self-start mr-3" src="{{program.poster}}" alt="{{ program.title }} poster">
        <div class="media-body">
            <h1 class="mt-0">{{ program.title }}</h1>
            <p>{{ program.summary }}</p>
            <p>Catégorie : {{ program.category.name }}</p>
            <h2>Voir les saisons disponible :</h2>
            <ul>
            {% for season in seasons %}
                <li><a class="btn btn-secondary" href="{{ path('program_season_show', {programId: program.id, seasonId: season.id}) }}">Saison {{ season.number }}</a></li>
            {% endfor %}
            </ul>
        </div>
    </div>

    <a class="btn btn-primary" href="{{ path('program_index') }}">Retour à la liste des programmes</a>
{% endblock %}